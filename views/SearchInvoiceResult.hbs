<!DOCTYPE html>
<html>
<head>
   <style>
        .invoice-info-box {
          background-color: #FFFFFF;
          border-radius: 10px;
          border: solid 2px #00B4CC;
          padding: 15px;
          margin: auto;
          box-shadow: rgba(0, 0, 0, 0.2) 0px 12px 28px 0px, rgba(0, 0, 0, 0.1) 0px 2px 4px 0px, rgba(255, 255, 255, 0.05) 0px 0px 0px 1px inset;
          color: black;
          width: calc(30% - 20px); 
          display: inline-block;
          position: absolute;
          top: 50%;
          left: 50%;
          transform: translate(-50%, -50%);
          text-align:center;
        }
       
        .invoice-info-box h3 {
          font-size: 1.2rem;
          margin: 0;
        }
        .invoice-info-box ul {
          list-style: none;
          padding: 0;
        }
        .invoice-info-box li {
          margin: 5px 0;
        }
        
      </style>
</head>
<body>
  
  <div class="invoice-info-box">
    <h3> Sales Information</h3>
    <ul>
     <li><strong>Invoice ID:</strong> {{invoice.[Invoice ID]}}</li>
    <li><strong>Branch:</strong> {{invoice.Branch}}</li>
    <li><strong>City:</strong> {{invoice.City}}</li>
    <li><strong>Customer Type:</strong> {{invoice.[Customer type]}}</li>
    <li><strong>Product Line:</strong> {{invoice.[Product line]}}</li>
    <li><strong>Name:</strong> {{invoice.name}}</li>
    <li><strong>Image:</strong> <img src="{{invoice.image}}" alt="Product Image" height="50" width="40"></li>
    <li><strong>Unit Price:</strong> {{invoice.[Unit price]}}</li>
    <li><strong>Quantity:</strong> {{invoice.Quantity}}</li>
    <li><strong>Tax 5%:</strong> {{invoice.[Tax 5%]}}</li>
    <li><strong>Total:</strong> {{invoice.Total}}</li>
    <li><strong>Date:</strong> {{invoice.Date}}</li>
    <li><strong>Time:</strong> {{invoice.Time}}</li>
    <li><strong>Payment:</strong> {{invoice.Payment}}</li>
    <li><strong>COGS:</strong> {{invoice.cogs}}</li>
    <li><strong>Gross Income:</strong> {{invoice.[gross income]}}</li>
    <li><strong>Rating:</strong> {{invoice.Rating}}</li>
        
    </ul>
  </div>
</body>
</html>
